apiVersion : apps/v1
kind: Deployment
metadata:
  name: user-service-deployment
  labels:
    app: e-commerce-platform-user-service
spec:
  replicas: 1
  selector:
    matchLabels:
      project: e-commerce-platform-user-service
  template:
    metadata:
      labels:
        project: e-commerce-platform-user-service
    spec:
      containers:
        - name: user-service
          image: ivangorbunovv/e-commerce-platform:user-service
          ports:
            - containerPort: 8081
          env:
            - name: SPRING_APPLICATION_JSON
              value: >
                {
                 "spring.config.import" : "configserver:http://config-server:8888"
                }

---
apiVersion: v1
kind: Service
metadata:
  name: user-service
  labels:
     env  : dev
     owner: IvanGorbunov
spec:
  selector:
    project: e-commerce-platform-user-service
  ports:
    - name      : user-service-listener
      protocol  : TCP
      port      : 8081
      targetPort: 8081
  #type: CLusterIP