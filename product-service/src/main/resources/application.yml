server:
  port: 8082

spring:
  application:
    name: product-service
  datasource:
    url: jdbc:postgresql://localhost:5432/e-commerce-platform-product-service
    username: postgres
    password: root
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update
